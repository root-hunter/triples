[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
plotters = "0.3.7"
triples = { workspace = true}

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
target-cpu = "native"